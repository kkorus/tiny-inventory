# Example variables for local development.
# The API reads server/.env; copy from server/.env.example

# For Docker, defaults are in docker-compose.yml. Override only if needed:
# nothing required for the default assignment stack.
